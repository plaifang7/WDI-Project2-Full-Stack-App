<div class="userBox">
    <h1 class="userShow">{{userProf.name}}</h1>
    <img class="userPicProf" src="{{userProf.image}}" alt="{{userProf.name}}">
    <p>Favorite Park: {{userProf.favPark}}</p>

    <div class="dogBox">
       
        <h3>{{dogProf.dog}}</h3>
        <img class="dogShow" src="{{dogProf.image}}" alt="{{dogProf.name}}">
        <p>Breed: {{dogProf.breed}}</p>
        <p>Age: {{dogProf.age}}</p>
        <p>About {{dogProf.name}}: {{dogProf.description}}</p>
    </div>

<button class="editButton"><a href="/users/{{userProf._id}}/edit">Edit User</a></button>
</div>